name: Build

on:
  push:
    branches: [main]
    tags: ["v*"]
  pull_request:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: windows-latest
    outputs:
      versionName: ${{ steps.build.outputs.versionName }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Fetch toolchain
        shell: pwsh
        run: .\module\fetch-tools.ps1

      - name: Restore signing keystore (if secret configured)
        shell: pwsh
        env:
          KEYSTORE_B64: ${{ secrets.KEYSTORE_B64 }}
        run: |
          if ($env:KEYSTORE_B64) {
            [System.IO.File]::WriteAllBytes(
              (Join-Path $env:GITHUB_WORKSPACE "paddington.keystore"),
              [System.Convert]::FromBase64String($env:KEYSTORE_B64))
            Write-Host "Signing keystore restored from secret."
          } else {
            Write-Host "No KEYSTORE_B64 secret set; build.ps1 will generate a throwaway keystore."
          }

      - name: Build APK
        id: build
        shell: pwsh
        env:
          RUN_NUMBER: ${{ github.run_number }}
        run: |
          $versionName = "0.0.$env:RUN_NUMBER"
          $versionCode = $env:RUN_NUMBER
          .\module\build.ps1 -VersionName $versionName -VersionCode $versionCode
          Write-Output "versionName=$versionName" >> $env:GITHUB_OUTPUT

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: paddington-apk
          path: dist/paddington-${{ steps.build.outputs.versionName }}.apk
          if-no-files-found: error

  release:
    needs: build
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    steps:
      - name: Download APK artifact
        uses: actions/download-artifact@v4
        with:
          name: paddington-apk
          path: release

      - name: Publish GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: release/paddington-${{ needs.build.outputs.versionName }}.apk
          generate_release_notes: true
